<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fervi.exptrip">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/welcome_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginPage"
            android:label="@string/login_page" />
        <activity
            android:name=".RegisterPage"
            android:label="@string/register_page" />
        <activity
            android:name=".PlanPage"
            android:label="@string/planner_page" />
        <activity
            android:name=".CreatePlanPage"
            android:label="@string/create_plans"></activity>
        <activity android:name=".CreatePlan2"></activity>
    </application>

</manifest>